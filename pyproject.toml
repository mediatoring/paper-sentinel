[project]
name = "paper-sentinel"
version = "0.1.0"
description = "Local AI-curated arXiv research radar"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
